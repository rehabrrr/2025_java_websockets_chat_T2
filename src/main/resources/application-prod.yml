spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/t2webchat
    username: postgres
    password: 123
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  liquibase:
    enabled: true
    change-log: classpath:/db-migrations/postgresql/init.yml
    database-change-log-lock-table: lb_databasechangeloglock
    database-change-log-table: lb_databasechangelog
